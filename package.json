{
  "name": "clear",
  "version": "0.0.0",
  "private": true,
  "description": "OpenTelemetry observability shared by people and their agents",
  "repository": "https://github.com/quick007/clear",
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "benchmark:grafana": "vp exec node video/benchmarks/compare-webmcp-grafana.mjs",
    "dev": "vp run console#dev",
    "media:prepare:hero": "vp exec node video/scripts/prepare-codex-board-hero.mjs",
    "media:prepare:benchmark": "vp exec node video/scripts/prepare-benchmark-card.mjs",
    "media:render": "vp exec node video/scripts/render-devpost-assets.mjs",
    "media:validate": "vp exec node video/scripts/validate-devpost-assets.mjs",
    "media:validate:manifest": "vp exec node video/scripts/validate-devpost-assets.mjs --manifest-only",
    "ready": "vp check && vp run -r test && vp run -r build",
    "test": "vp run -r test"
  },
  "devDependencies": {
    "sharp": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.24.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
